# ASL Stereo Recognition runtime and test dependencies.
# Requires CPython 3.11.x; other Python versions are outside the validated architecture.
# CPU-only PyTorch wheels keep deployment compatible with mid-range consumer CPUs.
# CPU wheel index: https://download.pytorch.org/whl/cpu
--extra-index-url https://download.pytorch.org/whl/cpu

numpy<2.0.0
scipy>=1.12.0
protobuf==4.25.3
pyyaml>=6.0

opencv-python>=4.8.0
opencv-contrib-python>=4.8.0
mediapipe==0.10.21
PyQt5==5.15.10

torch==2.3.0+cpu
torchvision==0.18.0+cpu

h5py>=3.10.0
tqdm>=4.66.0
scikit-learn>=1.4.0

pytest>=8.0
